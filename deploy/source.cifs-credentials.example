# Placeholder-only mount.cifs credentials. Install the private site copy as
# /etc/rosbag-analyser/source.cifs-credentials, owned by root:root, mode 0600.
username=REPLACE_WITH_READ_ONLY_SHARE_ACCOUNT
password=REPLACE_WITH_PRIVATE_PASSWORD
# Omit domain when the server does not require one.
domain=REPLACE_WITH_DOMAIN
